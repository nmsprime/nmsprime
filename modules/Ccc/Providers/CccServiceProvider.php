<?php

namespace Modules\Ccc\Providers;

use Illuminate\Support\ServiceProvider;

class CccServiceProvider extends ServiceProvider
{
    /**
     * Indicates if loading of the provider is deferred.
     *
     * @var bool
     */
    protected $defer = true;

    /**
     * The artisan commands provided by this module
     */
    protected $commands = [
        'Modules\Ccc\Console\CreateConnectionInformations',
    ];

    /**
     * Boot the application events.
     *
     * @return void
     */
    public function boot()
    {
        $this->registerTranslations();
        $this->registerConfig();
        $this->registerViews();
    }

    /**
     * Register the service provider.
     *
     * @return void
     */
    public function register()
    {
        $this->commands($this->commands);
    }

    /**
     * Register config.
     *
     * @return void
     */
    protected function registerConfig()
    {
        $this->publishes([
            __DIR__.'/../Config/config.php' => config_path('ccc.php'),
        ]);
        $this->mergeConfigFrom(
            __DIR__.'/../Config/config.php', 'ccc'
        );
    }

    /**
     * Register views.
     *
     * @return void
     */
    public function registerViews()
    {
        $viewPath = base_path('resources/views/modules/ccc');

        $sourcePath = __DIR__.'/../Resources/views';

        $this->publishes([
            $sourcePath => $viewPath,
        ]);

        $this->loadViewsFrom(array_merge(array_map(function ($path) {
            return $path.'/modules/ccc';
        }, \Config::get('view.paths')), [$sourcePath]), 'ccc');
    }

    /**
     * Register translations.
     *
     * @return void
     */
    public function registerTranslations()
    {
        $langPath = base_path('resources/lang/modules/ccc');

        if (is_dir($langPath)) {
            $this->loadTranslationsFrom($langPath, 'ccc');
        } else {
            $this->loadTranslationsFrom(__DIR__.'/../Resources/lang', 'ccc');
        }
    }

    /**
     * Get the services provided by the provider.
     *
     * @return array
     */
    public function provides()
    {
        return [];
    }
}
